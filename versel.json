{
    "builds": [
        {
            "src": "/api/index.py",
            "use": "@now/python",
            "config": { "excludeFiles": "public/**" }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.py"
        }
    ]
}